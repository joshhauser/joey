import { DISCOVER_PATH } from '@/shared/constants';
export class Client {
  static getTvShows() {
    const criterias = this.prepareCriterias({});
  }

  private static prepareCriterias(criterias: any) {
    return {};
  }
}
